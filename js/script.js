let time = 2000,
    currentImageIndex = 0,
    images = document.querySelectorAll('#slider img')
    max = images.length;

function start(){
    setInterval(() => {}, time)
}

window.addEventListener("load", start)